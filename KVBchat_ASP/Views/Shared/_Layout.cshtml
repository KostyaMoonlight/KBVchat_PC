<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("KVBchat", "User", "User", new { id = -1, area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "User", "User", new { id = -1, area = "" }, null)</li>
                </ul>               
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Friends", "GetFriends", "Friends", new { area = "" }, null)</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Add group", "AddGroup", "Group", new { area = "" }, null)</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Messages", "Messages", "Message", new { area = "" }, null)</li>
                </ul>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Search", "UserSearch", "UserSearch", new { area = "UserSearchEngine" }, null)</li>
                </ul>
                
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.Action("UserLogin", "Login", new { area = "Authentication" })</li>
    </ul>

            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/global")
    @Scripts.Render("~/bundles/globalLastFile")
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script> 
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)


</body>
</html>
